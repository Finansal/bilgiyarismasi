<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.burakkarabiyik.bilgiyarismasi">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ico"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"

        android:theme="@style/Theme.AppCompat.Light.NoActionBar">

        <activity android:name=".Giris">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Kayitol" />
        <activity android:name=".Anasayfa" />
        <activity android:name=".Profil" />
        <activity android:name=".Ayarlar" />
        <activity android:name=".Kategoriler" />
        <activity android:name=".SoruEkle" />
        <activity android:name=".Sonuc" />
        <activity android:name=".Butunkategoriler" />
        <activity android:name=".SifremiUnuttum"></activity>
    </application>

</manifest>